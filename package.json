{
  "private": true,
  "scripts": {
    "workspace:clean": "git clean -Xdf",
    "workspace:ci": "nx run-many -t build,lint,test",
    "prepare": "husky"
  },
  "workspaces": [
    "packages/development/*",
    "packages/production/*"
  ]
}
